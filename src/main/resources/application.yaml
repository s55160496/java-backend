Spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: syspass

    jpa:
      hibernate:
        ddl-auto: update

  Servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 5MB
